{
  "name": "abhi-ludo-offline",
  "version": "1.0.0",
  "private": true,
  "description": "ABHI LUDO - real playable offline Ludo Android app",
  "scripts": {
    "prepare-web": "node -e \"const fs=require('fs');fs.mkdirSync('www',{recursive:true});for(const f of ['index.html','style.css','game.js'])fs.copyFileSync(f,'www/'+f)\"",
    "android": "npm run prepare-web && npx cap add android && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.2",
    "@capacitor/android": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}