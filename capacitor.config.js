const { CapacitorConfig } = require("@capacitor/cli");
const config = {
  appId: "com.abhiludo.offline",
  appName: "ABHI LUDO",
  webDir: "www",
  bundledWebRuntime: false
};
module.exports = config;